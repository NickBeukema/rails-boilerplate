<form {{action "authenticate" on="submit"}}>
  <div class="form-group row">
    <label for="identification" class="col-4 col-form-label">Login</label>
    <div class="col-8">
      {{input name="identification" classNames='form-control' value=identification placeholder="Enter Login"}}
    </div>
  </div>

  <div class="form-group row">
    <label for="password" class="col-4 col-form-label">Password</label>
    <div class="col-8">
      {{input name="password" classNames="form-control" value=password type="password" placeholder="Enter Password"}}
    </div>
  </div>

  <div class="form-group row">
    <div class="offset-sm-4 col-sm-8">
      <button type="submit" class="app-login btn btn-primary">Login</button>
    </div>
  </div>
</form>

{{#if errorMessage}}
  {{errorMessage}}
{{/if}}
